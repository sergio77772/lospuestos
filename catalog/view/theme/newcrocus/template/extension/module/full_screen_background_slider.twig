<script type="text/javascript">
	$.vegas('slideshow', {
	  delay: 8000,
	  backgrounds:[
	  	{% for sliders in slider['content'] %} 
	 		{% if (sliders[language_id]['status'] == 1) %} 
	 			{{ "{src : 'image/" ~ sliders[language_id]['slider'] ~ "', fade: 500}," }}
	 		{% endif %}
	 	{% endfor %}
	 	
	  ]
	})('overlay');
</script>